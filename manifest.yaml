ref: fedora/${basearch}/coreos/okd
include: fedora-coreos-okd.yaml

releasever: "30"

rojig:
  license: MIT
  name: fedora-coreos
  summary: Fedora CoreOS testing

repos:
  - fedora-coreos-pool
  - fedora
  - fedora-updates
  - crio

add-commit-metadata:
  fedora-coreos.stream: okd
